node_modules/
.next/
out/
.turbo/
.env
.env.*
!.env.example
target/
dist/
.DS_Store

.vercel
.env*
supabase/.temp
.gstack/
!.env.example

# 테스트 커버리지 리포트
coverage/

# 원본 에셋 아카이브(zip)는 배포 대상이 아니다 — 실제로 쓰는 건 추출된 PNG 스프라이트고,
# public/에 두면 Vercel이 원본 팩을 공개 URL로 그대로 재배포하게 된다(에셋 팩 라이선스가
# 원본 재배포를 금지하는 경우가 많다). 로컬 파일은 그대로 두고 추적만 해제한다.
apps/web/public/*.zip
