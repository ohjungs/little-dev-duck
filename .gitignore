node_modules/
.next/
out/
.turbo/
.env
.env.*
!.env.example
target/
dist/
.DS_Store

.vercel
.env*
supabase/.temp
.gstack/
!.env.example

# 테스트 커버리지 리포트
coverage/

# /loop-eng 단일 인스턴스 락 — 머신·세션 지역 상태라 공유 대상이 아니다
.claude/loop-eng.lock

# 원본 에셋 아카이브(zip)는 배포 대상이 아니다 — 실제로 쓰는 건 추출된 PNG 스프라이트고,
# public/에 두면 Vercel이 원본 팩을 공개 URL로 그대로 재배포하게 된다(에셋 팩 라이선스가
# 원본 재배포를 금지하는 경우가 많다). 로컬 파일은 그대로 두고 추적만 해제한다.
apps/web/public/*.zip

# 2026-07-29 (V-010): DB 덤프 — 개인 대화 포함, 커밋 금지
backup/

# 2026-07-31 : 보안 - 개인정보 - 화면검증스크린샷
# 이 저장소는 PUBLIC이다. loop-eng 4-1은 "이번 Task가 건드린 화면 전부"를 찍으라고 지시하는데,
# 로그인 상태의 대시보드에는 이메일 주소·할일·메모 같은 개인정보가 그대로 찍힌다
# (실제로 2026-07-30 ai-feature-gate/dashboard 두 장이 공개돼 있었다).
# 스크린샷은 회귀 비교용 로컬 산출물이므로 추적하지 않는다. 매니페스트(index.md)는 URL·콘솔
# 로그만 담아 안전하므로 계속 추적한다.
docs/loop-eng/screenshots/**/*.png
