# 사용자 피드백 2026-07-26

사용자가 화면을 직접 써 보고 남긴 항목이다. `feedback-2026-07-25.md`의 후속이며,
**추측 후보보다 이 목록이 우선한다**(사용자가 말로 요구한 미완 항목).

이 문서는 **원문 보존 + 처리 상태**만 담는다. 남은 항목의 설계·착수 근거는
[`plans/phase_30.md`](plans/phase_30.md)에 있다.

상태 표기: ✅ 완료(배포됨) · 🟡 부분 · ⬜ 미착수 · ⏸ 확인 불가(사유 명시)

---

## 1. 대시보드

| # | 원문 | 상태 | 처리 |
|---|---|---|---|
| 1-1 | 왼쪽상단 로고나 Little Dev Duck 누르면 대쉬보드로 돌아오게해 | ✅ | **이미 되어 있었다** — 사이드바 Brand가 `<Link href="/">`(AppNav.tsx:92). 접힌 상태 로고도 동일 |
| 1-2 | 관리자 창에서 대쉬보드 구성을 바꿀수있으면 좋겠고 카드형식으로 움직일수있으면 | ✅ | 관리자 → 대시보드 구성. 위/아래 버튼으로 순서 변경(`277ca92`) |
| 1-3 | 오리가 자율적으로 말을걸고 물어보고시키기 + 이미지랑 통합해서 자연스럽게 대화 | ⬜ | Phase 30 T1 |
| 1-4 | 할일+습관+뽀모도로+메모+캘린더 등 기존 모든 기능을 오리랑 대화하며 작업예약·사용 | 🟡 | 도구 6종은 이미 있고 발화 라우터도 고쳐져 있다. **뽀모도로·작업예약이 없다** → Phase 30 T2 |
| 1-5 | 카드 형태의 대쉬보드 내 기능들을 보이거나 안보이게 하는 기능을 관리자 안에 | ✅ | 관리자 → 대시보드 구성에서 카드별 숨김(`277ca92`) |
| 1-6 | 왼쪽위 작은오리 이미지를 움직이는 오리 영상으로 대체 | ⬜ | Phase 30 T5 |

## 2. 페이지

| # | 원문 | 상태 | 처리 |
|---|---|---|---|
| 2-1 | 가져오기 이런걸보면 전부 글자가 깨지고 이상해 | ✅ | 원인은 파일이 아니라 **읽는 방식**이었다 — `File.text()`는 무조건 UTF-8. CP949/UTF-16 판별 추가(`0ddcf12`) |
| 2-2 | 노션처럼 템플릿 선택 + 마켓플레이스에서 가져오기 | ⬜ | Phase 30 T3 |
| 2-3 | 처음 페이지 생성 시 템플릿 선택 — 회의록·일기·포트폴리오·회고록 | ✅ | 선택 메뉴는 이미 있었고 **일기·포트폴리오·회고록이 없어** 추가(`0ddcf12`) |
| 2-4 | 내가 만든 페이지 내용을 오리가 인식·기억하고 대화, RAG 학습 | ✅ | **이미 되어 있었다** — 저장할 때마다 `pageEmbedText`로 재색인(PageEditor.tsx:317) |
| 2-5 | AI 작문 도우미를 오리가 돕는 형태로(오리가 생각중…) + LLM 기능 확장 | ✅ | 오리 표시 + 액션별 상태 문구 + 액션 3종 추가(`0ddcf12`) |
| 2-6 | 페이지 안에 엑셀 + 파워포인트 + 워드 기능 *(대화 중 추가 요청)* | ⬜ | Phase 30 T4 — **범위 합의 필요** |

## 3. 통계

| # | 원문 | 상태 | 처리 |
|---|---|---|---|
| 3-1 | 자주 방문하는 페이지, 평균 방문횟수, 방문 시간을 로그로 | ✅ | 통계 → 로그 탭(`227721c`) |
| 3-2 | 에러로그, 배치로그, 작업한 로그, 행동 로그 통계 | ✅ | 같은 탭. **새 테이블 없이** `action_log`에 이름 규칙으로 담았다(`227721c`) |

## 4. 뉴스

| # | 원문 | 상태 | 처리 |
|---|---|---|---|
| 4-1 | velog.io 추가했는데 못알아먹고 뉴스를 못가져오네 | ✅ | velog 홈은 RSS 링크를 광고하지 않고 피드가 **다른 도메인**(v2.velog.io)에 있어 자동 발견 불가였다. 사이트 규칙 + 관용 경로 폴백(`5a43f2a`) |
| 4-2 | 카드형식 제목-요약, 누르면 읽음, 저장·스크랩·복사·공유·원문보기 | ✅ | 저장·스크랩·복사·원문은 이미 있었고 **카드 클릭 읽음 토글** 추가(`5a43f2a`) |
| 4-3 | 주식/부동산 현황은 왜 못가져오지? | ✅ | 후보 13개를 실제로 요청해 **파싱되는 8개만** 추가(`5a43f2a`). 단 이건 **기사** 피드다 — 시세·호가 수치는 RSS로 오지 않는다 |
| 4-4 | 추천 피드에 여러가지 추천 계속 갱신 | ✅ | 주제 7개로 확장 + 주제 단위 일괄 추가(`5a43f2a`) |
| 4-5 | 인기많고 조회수 많은 최근거를 대쉬보드에 TOP3 | ✅ | **조회수는 우리에게 없는 데이터라 지어내지 않았다.** "몇 개 매체가 같은 사건을 다뤘나"를 근거로 쓰고 화면에 그대로 밝힌다(`5a43f2a`) |

## 5. 오피스

| # | 원문 | 상태 | 처리 |
|---|---|---|---|
| 5-1 | 사운드가 이상해, 상용 제품이나 무료 에셋 사운드로 | ✅ | Kenney CC0 6개로 교체 + **드론 BGM 제거**(사인파 3화음을 끊김 없이 울리고 있었다). `c9f532b` |
| 5-2 | 사물들 에셋이 벽을 넘어가는등 UI가 이상해 | ✅ | 자산 43개를 재보니 타일 32px인데 **32×32 자산 20개가 64×64로** 그려지고 있었다(`243e8c6`) |
| 5-3 | 실제 에이전트와 매칭, 일 안 하면 쉬는중 표시 | ✅ | 실제 업무 유무로 상태 판정, 없으면 "쉬는 중"(`5a43f2a`) |
| 5-4 | 실제 개발중인 서버와 연결해서 에이전트들 일하게 | ✅ | **원천은 앱 내부 작업 상태로 합의**(할 일·페이지·습관·일정·뽀모도로). 이제 그것만 보인다 |
| 5-5 | 자금·수익 게임성 요소 불필요, "수고했어" 불필요, 퇴근 실제 동작 | ✅ | 재정 시뮬·급여·만족도·평판·MVP·"수고했어" 제거, 퇴근 버튼 추가(`243e8c6`) |
| 5-6 | 사장오리 좌우 위아래 모습변화·상호작용 변화가 없어 | ✅ | 시트를 픽셀로 열어 보니 **전 프레임이 같은 오른쪽 옆모습**이었고 `down`엔 빈 프레임 2칸이 재생됐다(깜빡임). 좌우 반전 + 프레임 상한(`243e8c6`) |
| 5-7 | 직원들이 뭐 바뀌면서 일하는척, 실제 일하는 것만 보이도록 | ✅ | `simulateNpcTasks`가 1분마다 10% 확률로 **없는 업무를 지어내고** 진행률·만족도를 난수로 흔들고 있었다. 생성기·템플릿 사전 삭제(`5a43f2a`) |

## 6. 설정 + 관리자

| # | 원문 | 상태 | 처리 |
|---|---|---|---|
| 6-1 | 내가말한 모든것들을 설정에서 제어, 기능추가설정 | 🟡 | 기능 토글 12종 구현. **설정 화면 통합 배치는 미착수** → Phase 30 T6 |
| 6-2 | 사용자별 개인화 + 관리자가 권한 부여, 껏다켯다 하는 식 | ✅ | 관리자 → 사용자 관리에서 사용자별 기능 토글(`277ca92`) |
| 6-3 | ADMIN, USER, CUSTOMER 권한 분류 | ✅ | 역할 3종 + RLS. **판정의 단일 출처는 RLS**, 화면은 숨기기만 한다(`277ca92`) |
| 6-4 | 왼쪽아래 계정정보도 프로필설정 가능하게 | ✅ | 관리자 → 내 프로필(`277ca92`) |

---

## 확인하지 못한 것 (정직하게)

| 항목 | 왜 |
|---|---|
| 5-1 사운드가 실제로 **좋은지** | ⏸ 코드로는 들을 수 없다. 음소거·번갈아 재생·실패 내성 같은 아래 계약만 테스트로 잠갔다 |
| 로그인 후 화면 전부 | ⏸ 로그인할 수 없어 육안 확인 불가 — `loop-eng/manual-verification.md` |
| 6번 전체 실동작 | ⏸ **마이그레이션 미적용** — 아래 참조 |

## 막힌 것 — 사용자만 풀 수 있다

DB 마이그레이션 **4건이 미적용**이고, 두 경로 모두 권한 게이트에 막혔다.

- Supabase MCP `apply_migration` → 차단
- `supabase db push` → 차단

상세와 실행 명령은 [`loop-eng/PENDING.md`](loop-eng/PENDING.md) 1절.
**그중 하나는 지금 열려 있는 보안 구멍이다**(로그인 없이 남의 XP·레벨 변경 가능).

## 이 세션의 배포 (6커밋)

| 커밋 | 내용 |
|---|---|
| `5a43f2a` | 뉴스 4-1~4-5 + 오피스 5-3·5-7 |
| `243e8c6` | 오피스 5-2·5-5·5-6 |
| `0ddcf12` | 페이지 2-1·2-3·2-5 |
| `c9f532b` | 오피스 5-1 |
| `277ca92` | 권한·기능 토글·대시보드 구성 (1-2·1-5·6-2·6-3·6-4) |
| `227721c` | 통계 로그 3-1·3-2 |

검증: core 693 · api 406 · web 362 tests GREEN / lint 0 error / next build 통과.
