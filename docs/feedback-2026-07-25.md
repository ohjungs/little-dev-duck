# 야간 자율 세션 피드백 처리 — 2026-07-25 (아침 확인용)

사용자가 라이브 사이트를 리뷰하며 다수 피드백을 남기고 취침(전면 자율 승인: "권한 필요한 건
알아서 승인, 필요한 것 알아서 설치, 내일 확인"). 아래는 처리 현황이다. **모든 커밋은 검증
(tsc+eslint+test GREEN) 후 main 푸시 → Vercel 자동 배포됨.**

## A. 완료·배포됨 (이번 세션)

| # | 항목 | 커밋 | 요지 |
|---|---|---|---|
| 1 | 오리 blur 키 고착 | d7154c1 | 캔버스 포커스 잃을 때 방향키 눌림 고착 방지(Phase 17 제약) |
| 2 | 오피스 키보드 트랩 | 9c4c981 | Tab 트랩 해소, Shift+Tab 탈출구(WCAG 2.1.2) |
| 3 | 오리 인사 인식 | (ruleReply) | "안녕/고마워/잘가"를 알아듣고 응답(LLM 쿼터 0) |
| 4 | 인사말 KST 시간대 | (page) | 밤인데 "좋은 오후 해" 뜨던 버그 — 서버 UTC→KST 계산, 날짜도 KST |
| 5 | 상단 퀵액션 버튼 제거 | (page) | 네비 중복이라 제거 |
| 6 | 뽀모도로 시간 옵션 | (pomodoro) | 25/50 두 개 → 15/25/50 프리셋 + 직접 입력(1~180분) |
| 7 | 할일 헤더 UI 깨짐 | (todo) | 제목이 세로로 찌부러지던 레이아웃 재구성 + "오늘 마감" 빈 상태 안내 |
| 8 | 인덱싱 자동화 | (duckchat) | "기존 메모·할일 인덱싱" 버튼 제거 → 최초 1회 자동 실행 |
| 9 | **오리 렌더링 교체** | 852b8ca | **49MB 3D GLB → SVG+CSS 애니메이션**(눈 깜빡임/두리번/기분별 표정/터치 찌부러짐/축하 도약). 즉시 로드, "깨짐" 해소 |

### 추가 완료 (iteration 3)
- **위젯 접기 버튼 겹침 수정**: 펼침 상태 접기(˅)가 위젯 자체 헤더 컨트롤과 상시 겹치던 것 →
  hover 시에만 노출.
- **애니메이션 오리 로고**(DuckLogo, SVG+CSS): 로그인의 이모지+png 중복 로고 제거, 마운트 시 "꽥"
  인트로 + hover 반응. 로그인·AppNav 좌상단·접힌 사이드바 통일(MGM 사자 인트로의 오리판 1차).
- **대시보드 오리를 픽셀아트 스프라이트로 교체**: SVG 병아리가 "못생겼다"는 피드백 → 기존 픽셀 오리
  시트(ducky_2, 우측면 뷰, Brysia 레퍼런스와 동일 계열)를 CSS 스프라이트 애니로. **Playwright로 시트
  레이아웃 실측**(row0=idle 2프레임, row1=걷기 6프레임)해 빈 프레임 밟던 버그 없이 매핑. 기분별
  애니(idle/뒤뚱걸음/기울임)+클릭 폴짝+레벨업 점프.
- **Modern Interiors 에셋 추출·커밋**: 오피스용 캐릭터/타일셋을 public/sprites/modern-interiors에 준비.
- **Playwright 시각 검증 워크플로 구축**: chromium 설치. 스프라이트/컴포넌트를 오프스크린 렌더→스크린샷→
  확인. 남은 오피스 비주얼 작업에 활용.

### 추가 완료 (iteration 4)
- **통계 페이지 탭 구조화**: 개요 / 할 일·습관 / 집중 3탭. 스탠드업·복사는 탭 위 전역 액션.
- **캘린더 월(月) 그리드**: 리스트만 있던 캘린더에 실제 달력 그리드(요일·일정점·오늘강조·월이동·
  날짜클릭 필터). 구글 일정 로딩은 토큰 재연동 선행 + 표시전용 fetch 라우트 필요라 후속.

### 추가 완료 (iteration 5)
- **설정 페이지 재배치**: 좁은 단일 컬럼 → 넓게(max-w-5xl) + CSS 다단(masonry) 2열. Google/GitHub/Gmail
  연동 카드 3개를 "외부 연동" 한 카드 행으로 통합. GitHub 기여 잔디를 대시보드→설정으로 이동.
- **에디터 폰트/자간 정돈**: BlockNote가 앱 폰트(Geist)를 상속하도록 + letter-spacing normal + 한글
  word-break keep-all + 이미지 컨테이너 초과 방지. (세밀 튜닝은 실사용 확인 필요.)

### 추가 완료 (iteration 6)
- **뉴스 RSS 자동 발견**: news.hada.io처럼 사이트 홈 URL을 등록하면 HTML만 와서 0건이던 근본 원인
  해결. 수집 시 항목 0개 + HTML이면 `<link rel=alternate type=rss>`를 찾아 실제 피드로 재수집하고
  feeds.url을 갱신(다음부턴 바로 수집). "등록했는데 못 가져옴" 해소. STDD 4케이스.
  - 후속: 주제별(IT/주식/부동산/AI) 큐레이션 피드 추천은 좋은 RSS 목록 필요라 별도. 웹크롤링·SNS는
    무료·ToS 밖(아래 C).

### 추가 완료 (iteration 7)
- **오피스 실시간 KST 시계 동기화**: 항상 8시 시작·빠른 게임 시뮬(1초=1분)을 실제 KST 시각 반영으로
  교체. 낮/밤 오버레이·HUD 시계가 실제 시간과 일치("게임처럼 보이지만 게임은 아닌" 오피스).
- **에이전트 상시 상주**: 실시간 밤이면 스케줄상 전원 퇴근→오피스가 비던 것을, AI 에이전트는 시간
  무관 상주하도록 출퇴근/퇴근 단계를 working으로 대체(밤에도 직원 보임, 낮 점심·휴식 배회는 유지).
  "직원 없지" 완화 + "에이전트들을 보기 위해" 취지 반영.
  - **남은 대형(스코핑)**: 오피스 타일셋 배경 전면 리메이크(Modern Interiors)는 시각적 반복 필요라
    Playwright 오프스크린 렌더로 타일 매핑 검증 후 단계 진행 예정. 실 Claude Code 에이전트 연동은
    Tauri sidecar(데스크톱) + 이벤트 소스 필요(대규모).

### 추가 완료 (iteration 8)
- **오피스 바닥/배경 버그 수정**: "배경 이상하지"의 원인 규명(Playwright로 PixelOfficeAssets 타일셋을
  격자 렌더). 바닥을 타일셋 (64,32) 등에서 샘플링했는데 그 좌표가 실제로는 의자/책상이라 바닥 전체가
  가구로 타일링됐음. 이 타일셋엔 전용 바닥 타일이 없어서, 바닥은 항상 절차적 단색(베이지 체크/복도/
  카펫)으로 그리도록 통일. 배경이 깔끔해짐.
  - **남은 오피스 시각(다음)**: 가구 매핑(TILESET_MAP)도 일부 좌표가 어긋남(예: Chair가 구름 셀).
    ~20종을 Playwright 셀 단위 렌더로 검증하며 재매핑 예정. 또는 Modern Interiors 타일셋으로 전면 교체.

### 추가 완료 (iteration 9)
- **오피스 가구·벽 렌더 정상화**: Playwright로 타일셋 셀을 검증하니 TILESET_MAP 좌표가 실제 가구와
  전부 어긋나 있었음(벽=주황 책상, 의자=구름 셀, 문=흰 패널 — 사용자 스크린샷의 주황/파랑 블록의 정체).
  깔끔한 개별 가구 PNG(Desk/Chair/Big-Plant/Big-Sofa/Vending-Machine 등 43종)가 이미 있어, TILESET_MAP을
  비워 가구는 개별 PNG로, 벽은 절차적 회색으로 통일. **목업 씬 Playwright 렌더로 깔끔한 사무실 확인**
  (베이지 바닥+회색 벽+모니터 책상+화분+소파+자판기+정수기). iter8 바닥 수정과 합쳐 "배경 이상하지"
  근본 해결.
  - **남은 오피스(스코핑)**: 방/구역 레이아웃 배치 정교화, Modern Interiors 캐릭터로 직원 교체(선택),
    실 Claude Code 에이전트 연동(Tauri sidecar 필요, 대규모).

### 추가 완료 (iteration 10)
- **오피스 직원(NPC) 안 보이던 버그 수정**: "직원 없지"의 정체 — drawHumanSprite가 sy=48(하늘/구름)을
  샘플링해 NPC가 구름 조각으로 렌더돼 사실상 안 보였음. Playwright로 타일셋 실측해 실제 캐릭터 3종이
  row 7(y=112)에 있음을 확인 → sy=112로 교정. 직원들이 제대로 보임(목업 렌더 확인).
  - **오피스 렌더 파이프라인 전체 정상화 완료**: 바닥(iter8)·가구·벽(iter9)·직원(iter10) 모두 타일셋 셀
    오매핑 버그였고 전부 수정됨. 남은 건 방/구역 배치 정교화와 실 에이전트 연동(대규모).
  - 알려진 소소한 잔여: 3번째 캐릭터 셀에 미세한 경계 아티팩트(1/3 부서). 기능 무해, 후속 미세조정.

### 추가 완료 (iteration 11)
- **오리 대화로 앱 기능 제어(1단계)**: "대화로 모든 기능 제어" 요청 착수. 외부 토큰 없이 항상 제공되는
  앱-내부 액션 어댑터(createTodo/createMemo, 둘 다 승인 게이트) 추가. "할일 추가해줘: 장보기",
  "메모 남겨줘"로 워크스페이스 직접 조작. /agent·/approve 양쪽 배선, 승인 카드 라벨 추가. STDD 4케이스.
  - **후속(확장 여지)**: 페이지 생성·할일 완료 처리·내부 캘린더 일정·습관 체크 등 액션 추가로 "디테일한
    부분까지" 확장 가능(각 api 함수를 도구로 노출).

### 오리 관련 상세
- 원인: 대시보드 위젯이 `/little_dev_duck.glb`(**49MB**)를 three.js로 로드 → 로딩 수 초 + 밝은
  조명에 색이 씻겨 흰 덩어리처럼 보임.
- 조치: 즉시 로드되는 SVG 아기오리 + CSS 애니메이션으로 교체(터치 반응·기분 표현 포함). 공개
  API/대사/유휴/방해금지 로직은 전부 보존.
- 미사용이 된 49MB GLB는 사용자 자산이라 **보존**(원하면 최적화 후 재활용 또는 삭제 가능).
- **후속(요청 반영 예정)**: "MGM 사자처럼 우는·두리번·딴청 애니 로고" — 현재 SVG가 1차 근사.
  더 고급 연출(Higgsfield 영상/Figma)은 외부 유료 서비스라 별도 결정 필요(아래 C 참조).

## B. 다음 처리 예정 (코드로 완결 가능, 자율 진행 중)

- **오피스 에셋 통합**: 사용자가 `Modern_Interiors_Free_v2.2.zip`(LimeZu, public/에 넣음) 제공.
  압축 해제 후 캐릭터(Adam/Alex/Amelia/Bob… 16x16 idle/run/sit) + 인테리어 타일셋을 office-draw에
  통합. 현재 맵/렌더가 어색한 문제 개선. **PLAYWRIGHT로 시각 확인하며 진행 예정.**
- 습관/할일 위젯 접기(˅) 동작 정리(내용만 사라지고 헤더 남는 문제).
- 통계 페이지 탭 구조화(항목별: 개요/할일·습관/집중/기록).
- 설정 페이지 좌우 2열 배치 + 연동 통합 UI(개별 버튼 → 한 곳에서 탭탭 연동) + 잔디를 설정으로 이동.
- 캘린더 위젯을 실제 월(月) 그리드로 + 구글 캘린더 연동 시 일정 로딩.
- 페이지 에디터(BlockNote) 이미지/자간 스타일 개선.

## C. 대형/스코핑 — 사용자 결정 또는 제약 있음 (정직 보고)

- **뉴스 주제별 수집 + 웹크롤링 + SNS 인용**: RSS 수집은 코드로 가능(현재 news.hada.io 0건은 RSS
  경로 문제일 수 있어 점검 예정). 그러나 **임의 사이트 웹크롤링/Velog 주제 수집/Instagram·X 인용**은
  (a) 무료 티어 밖 외부 크롤링 인프라 필요, (b) Instagram/X는 ToS상 스크래핑 금지·API 유료. →
  RSS/공개 피드 기반 주제 필터는 구현하되, SNS 스크래핑은 합법·무료 범위에서 대안 검토 필요.
- **오피스 실 에이전트 백엔드**("마케팅팀 등 모든 직원을 실제 에이전트로"): 멀티에이전트 부서
  시스템은 대규모 아키텍처(수일). 현재는 시뮬레이션. 실 Claude Code 이벤트 연동은 Tauri sidecar
  (데스크톱) 필요. → 단계적 스코핑 문서화 필요.
- **오리 대화로 모든 기능 제어**(할일/습관/설정 등 자연어 제어): 에이전트 도구를 앱 내부 액션으로
  확장하는 큰 작업. 도구 카탈로그 설계부터 필요.
- **애니메이션 로고(Higgsfield/Figma)**: 외부 유료 생성 서비스. SVG 애니로 1차 대체함.
- **구글 캘린더 만료**: "내가 뭐해야하지?"에 "Calendar 인증 만료" 뜬 건 토큰 만료 — 설정에서
  재연동 필요(사용자 몫, 코드는 정상 안내 중).

## D. 검증/운영 메모
- 스크린샷의 일부 문제(퀵액션 버튼 등)는 **배포 반영 전** 라이브 화면이라 곧 갱신됨.
- 전 커밋 CI GREEN 확인. 전 패키지 turbo test/lint GREEN.
