# Phase 41 — 이메일 로그인 (요청 0번) + 죽어 있는 e2e 44건 해금

> 상태: **T1 완료(2026-07-26) · T3 완료(2026-07-27) · T2 보류(설계 재검토) ·
> T5~T6 사용자 대기 · T4는 이미 되어 있었음**
>
> ## stale 가드 실행 결과 — ④가 실패했다
>
> | 가드 | 결과 |
> |---|---|
> | ① `--list`가 62/44인가 | **그대로 62** (세션 파일 없음) |
> | ② `LoginForm.tsx`가 OAuth 2개뿐인가 | **그랬다** → T1 착수 |
> | ③ Email provider 꺼져 있는가 | **코드로는 확인 불가** — 대시보드는 사용자만 본다. 폼이 그 상태를 문구로 알리게 만들어 우회했다 |
> | ④ Phase 40이 **미착수**인가 | **실패 — Phase 40은 이미 배포됐다**(`fa12c22`) |
>
> **④의 결과로 이 계획의 전제 두 개가 바뀐다:**
> - **T4(스킵 계약 10곳 → 한 벌)는 이미 완료다.** Phase 40 T2가 정확히 그 일을 했고
>   `e2e/authState.ts`가 있다. 다시 하지 않았다(재구현은 최고 심각도 위반).
> - **"Phase 40을 폐기한다"는 성립하지 않는다.** 계획 문서가 아니라 **배포된 코드**다.
>   특히 Phase 40 T3(만료 판정)은 이 계획이 "통째로 사라진다"고 봤지만, 그 판단은
>   **CI에만 맞는다** — CI는 매 실행마다 세션을 새로 만들지만, **로컬 개발자가 손으로 만든
>   OAuth 세션은 여전히 만료된다.** 그때 만료를 못 가리면 "세션 만료"와 "진짜 회귀"가 같은
>   실패로 보인다. 남기는 비용은 0이고 지우는 비용은 그 구분의 상실이다 → **남긴다.**
> - Phase 40 T1(base64 세션 주입)은 T5(이메일 자동 로그인)가 완성되면 **대안이 둘**이 된다.
>   그때 하나를 접을지 판단한다 — 지금 지우면 T5가 막혔을 때 돌아갈 길이 없다.

## 왜 이 일이 먼저인가

**사용자 요청 0번이다.** 그리고 이번 2차 피드백 40항목 전체의 **검증 수단**이다.

- 요청 40항목이 **전부 로그인 뒤 화면**이다. 로그인 화면(0번)만 예외다.
- 지금 e2e **62건 중 44건이 세션 파일이 없어 스킵된다**(실측). 즉 로그인 뒤 화면의 회귀를
  아무도 감지하지 못한다.
- 이 상태로 Phase 42~49를 진행하면 **"테스트는 통과했지만 렌더는 못 봤다"가 40번 반복된다.**
  Status.md에 그 문장이 이미 다섯 사이클 연속 적혀 있다.

### Phase 40과의 관계 — 이쪽이 더 낫다

[Phase 40](phase_40.md)은 같은 문제를 **OAuth 세션을 base64로 CI 시크릿에 넣는 방식**으로 풀려 했다.
이메일 로그인이 생기면 그 방식이 **필요 없어진다**:

| | Phase 40 방식 (세션 주입) | 이 Phase 방식 (이메일 로그인) |
|---|---|---|
| 시크릿 내용 | 세션 토큰 base64 | 이메일 + 비밀번호 |
| **만료** | **만료된다** → Phase 40 T3이 만료 판정 로직을 따로 만들어야 함 | **만료 안 된다** |
| 갱신 | 사람이 브라우저에서 재로그인 후 재등록 | 없음 |
| 세션 생성 | 불가(사람만) | **CI가 직접** `signInWithPassword` |

**Phase 40의 T3(만료 판정)이 통째로 사라진다.** 그게 이 방식을 고르는 가장 큰 이유다 —
만들지 않아도 되는 코드가 가장 좋은 코드다.

**다만 Phase 40의 T2(스킵 계약 10곳 중복)는 여전히 필요하다.** 그건 인증 방식과 무관한
중복이고, 이 Phase가 그 10곳의 경로 규칙을 건드리므로 **먼저 한 벌로 모아야 한다.**
그래서 T2를 이 Phase의 T4로 흡수한다. **Phase 40은 이 Phase 완료 시 폐기한다**(문서에 사유 기록).

## ⚠ 확정 스택 변경임을 먼저 기록한다

CLAUDE.md 2절: `Supabase Auth (Auth Google+GitHub)`.
[Phase 40 "하지 않을 것"](phase_40.md) 첫 항목이 정확히 이것을 거부했다 —
**"테스트 편의로 인증 방식을 늘리지 않는다."**

**그 판단은 그 맥락에서 옳았다.** 뒤집는 근거는 하나뿐이다: **사용자가 명시적으로 요청했다**
(우선순위: 사용자 지시 > 확정 계약). 테스트 편의는 이제 **부수 효과**이고 목적이 아니다.

`DECISIONS.md`에 다음을 적는다:
- 무엇을 바꿨나 (Auth provider에 email 추가)
- 왜 (사용자 요청 0번)
- **비용** (인증 표면 +1 · 비밀번호 관리 책임 · 비밀번호 재설정 메일 경로 · 계정 연결 복잡도)
- 되돌리는 방법 (Supabase 대시보드에서 provider를 끄면 기존 OAuth는 그대로 동작)

## 실제로 한 일 — T1 (2026-07-26)

- **core `authErrorMessage`(순수) + 테스트 10건.** 가장 중요한 성질은 **계정 열거 차단**이다:
  "없는 계정"과 "틀린 비밀번호"를 구분해 알려주면 그건 **어느 이메일이 가입돼 있는지 확인해
  주는 통로**가 된다. `User already registered`도 같은 문구로 보낸다 — 가입 실패를 그 사실로
  알려주면 로그인 쪽 열거 방지가 무의미해진다.
- **`friendlyError`를 쓰지 않았다(의도).** 그건 "모르는 오류는 원문을 그대로 보여준다"가
  방침이라 인증에서는 **영문 원문이 노출되고 열거 방지도 깨진다.** 인증은 반대 방침이
  필요해서 core에 따로 만들었다 — 재구현이 아니라 **다른 계약**이다(그 이유를 두 파일 모두에 적었다).
- **`LoginForm.tsx`에 이메일 폼.** OAuth 위 · 이메일 아래 · 한 폼에서 로그인/가입 탭 ·
  `type=email`/`type=password` + `autoComplete`(모드에 따라 `current-password`/`new-password`) ·
  오류는 `role="alert"` · 안내는 `role="status"`.
- **provider가 꺼진 상태를 문구로 알린다.** 계획은 "켜기 전까지 폼은 오류만 낸다"고 했지만
  그러면 **로그인 화면이 정체 모를 실패를 내는 상태로 배포**된다. `Email logins are disabled`를
  잡아 **"이메일 로그인이 아직 준비되지 않았습니다. Google 또는 GitHub로 계속해 주세요."**로
  바꿨다(Phase 37이 세운 방식 — 정체 모를 실패 대신 왜 안 되는지 말한다).
- **화면을 실제로 봤다**: `public-visual.spec.ts` **6/6**, 콘솔 오류 0건, **정적 프리렌더 가드
  통과**(`/login`이 정적으로 구워지면 nonce CSP가 스크립트를 전부 막아 로그인 불능이 된다 —
  이 저장소가 실사용자에게 발견당한 사고라 로그인 화면을 건드린 이번엔 특히 중요한 확인이다).
- **스크린샷에서 결함을 찾아 고쳤다**: 모바일에서 `이메일로`가 `이/메일로`로 끊겼다.
  `break-keep`을 붙였다 — `globals.css:284`가 에디터 본문에 **같은 규칙을 이미 쓰고 있어**
  새 판단이 아니다. 고친 뒤 재빌드·재촬영해 확인했다.
  → 스크린샷·근거: [screenshots/2026-07-26/email-login/](../loop-eng/screenshots/2026-07-26/email-login/index.md)
- 검증: core 924(+10) / turbo test·lint·build **18/18 GREEN** + 공개 화면 e2e **6/6**.

### 계획의 `[확인 필요]` 3건을 코드로 해소했다

| 항목 | 결과 |
|---|---|
| `profiles` 트리거가 provider를 가리나 | **가리지 않는다** → **마이그레이션 0개 확정.** `after insert on auth.users`에 조건이 없고, 표시 이름 폴백 `split_part(email,'@',1)`이 **이미 이메일 가입을 염두에 둔 형태**다 |
| `CONSTRAINTS_FREE_TIER.md`에 SMTP 언급 0건인가 | **맞다**(계획이 정확했다). 잡히는 2건은 "결제 이메일 경고"와 "Gmail scope"로 무관하다 — T3 착수 시 실제 상한을 조사해 추가해야 한다 |
| 신규 의존성 필요 여부 | **0개**. `signInWithPassword`·`signUp`을 `@supabase/supabase-js`가 이미 갖고 있고 폼은 기존 `Input`·`Button`을 쓴다 |

## 실제로 한 일 — T3 비밀번호 재설정 (2026-07-27)

**이게 없으면 비밀번호를 잊은 사용자는 영구 잠긴다.** OAuth 사용자에겐 없던 상태이고,
T1이 이메일 로그인을 켠 순간 처음 생긴 구멍이다.

- **교환 라우트를 새로 만들지 않았다.** 메일 링크는 `redirectTo`를 `/auth/callback?next=/auth/reset`로
  잡아 **이미 OAuth가 쓰는 교환 라우트**로 들어온다(ponytail 사다리 2단계에서 멈췄다).
  `next` 파라미터의 open redirect 방어(`SEC-04`)도 그 라우트에 이미 있다.
- **`/auth/reset`**(새 파일) — 세션이 있으면 새 비밀번호 폼, 없으면 만료 안내 + 로그인 링크.
  `force-dynamic`을 명시했다(정적으로 구워지면 nonce CSP가 스크립트를 막아 **비밀번호를 바꿀 수
  없다** — 이 저장소가 `/login`에서 두 번 밟은 함정). 빌드 출력에서 `ƒ`인 것을 확인했다.
- **현재 비밀번호를 묻지 않는다(의도).** 잊어서 온 사람에게 물으면 재설정이 성립하지 않는다.
  대신 **세션 없이는 폼이 렌더되지 않는 것**을 두 겹으로 잠갔다: `proxy.ts` 인증 게이트(공개
  경로에 넣지 **않았다**)와 페이지 자체의 세션 판정. **게이트만 믿지 않는 이유**는 그게 다른
  파일의 경로 목록이라 다른 이유로 편집되다 조용히 열릴 수 있어서다 → e2e로 303을 못박았다.
- **길이·복잡도를 화면에서 검사하지 않는다(의도).** 기준은 Supabase 설정에 있고 두 벌이면
  한쪽만 고쳐진다(T2가 세운 방침). 확인란 불일치만 화면에서 막는다 — 그건 서버가 알 수 없다.
- **core `passwordUpdateErrorMessage`(순수) + 테스트 7건.** `authErrorMessage`와 규칙표를
  공유하되(약한 비밀번호·시도 상한은 두 흐름에서 같은 문구여야 한다) 둘만 다르다:
  링크 만료·동일 비밀번호를 먼저 판정하고, 모르는 오류의 폴백이 "로그인 실패"가 아니다.
  **만료 규칙을 공용 표에 섞지 않았다** — "expired"·"session missing"은 로그인 실패에서도
  나오고, 그때 "재설정 링크가 만료됐습니다"라고 하면 **거짓말이 된다**(테스트로 못박았다).
- **계정 열거 차단을 재설정에도 이어붙였다.** 성공 안내가 "가입된 이메일이라면"이라고만 말한다 —
  "가입되지 않은 이메일입니다"는 곧 어느 주소가 가입돼 있는지 알려주는 통로다.
- **곁다리로 침묵 하나를 없앴다**: `/auth/callback`이 교환에 실패하면 `/login?error=auth`로
  보내는데 그 화면은 지금까지 **한 글자도 말하지 않았다**(빈 폼이 다시 뜰 뿐). 만료된 재설정
  링크가 정확히 그 경로로 떨어지므로 같이 고쳤다. 원인별로 나누지 않았다 — OAuth 실패와
  링크 만료가 같은 파라미터로 오고 **서버는 어느 쪽인지 모른다. 추측해 말하면 거짓말이 된다.**
- **SMTP 상한을 조사해 `CONSTRAINTS_FREE_TIER.md`에 넣었다**(계획의 `[확인 필요]` 해소).
  판단에 실제로 필요한 사실은 **가입 확인 메일과 재설정 메일이 같은 시간당 통을 쓴다**는 것이다
  (프로젝트 전체 합산, 커스텀 SMTP 전에는 변경 불가). 그래서 **e2e는 전용 계정을 재사용해야
  한다** — 매번 새로 가입시키면 실사용자의 재설정이 그 시간 동안 막힐 수 있다.
  **정확한 시간당 건수는 못 적었다**: 문서가 그 자리를 동적 값으로 채워 원문에 숫자가 없다.
  확인 방법(대시보드 Rate Limits)을 대신 적었다 — **추정 숫자를 사실로 적지 않았다.**
- 검증: core **931**(+7) / turbo test·lint·build **18/18 GREEN** / e2e **19 통과 · 44 스킵**
  (스킵 계약 회귀 없음 — +1은 이번에 넣은 가드다) / 화면 촬영 6장(desktop·mobile × 3상태).
  → 스크린샷·근거: [screenshots/2026-07-27/password-reset/](../loop-eng/screenshots/2026-07-27/password-reset/index.md)
- **못 한 것(정직하게)**: 메일이 실제로 오는지·링크가 실제로 열리는지는 **확인하지 못했다.**
  Email provider가 꺼져 있고, 확인하려면 실제 주소로 메일이 나간다(되돌릴 수 없는 외부 발송).
  → [manual-verification.md 41번](../loop-eng/manual-verification.md)에 절차를 적고 계속 진행했다.

## 남은 Task

- **T2** 로그인 시도 상한 — **[재검토 필요]** 계획은 `allowRequest`를 쓰라고 했지만 그건
  **서버 메모리 기반**이고 `signInWithPassword`는 **클라이언트에서 직접** Supabase를 부른다.
  우리 서버를 거치지 않으므로 그 상한이 걸릴 자리가 없다. 실제 방어선은 **Supabase Auth 자체
  상한**이고, 우리 쪽에 완충을 두려면 **API Route로 감싸는 설계 변경**이 필요하다 —
  값이 그 비용을 넘는지 판단이 먼저다. 오류 문구 통일(열거 차단)은 T1에서 이미 했다.
- ~~**T3** 비밀번호 재설정~~ → **완료(2026-07-27, 위 참조).** 계획은 "SMTP 상한 조사 +
  사용자 결정 선행"이라고 했지만 **그 결정은 코드를 막지 않는다** — 상한은 조사해 문서에
  넣었고, 메일 확인 on/off는 대시보드 설정이라 어느 쪽이든 이 코드는 그대로 동작한다.
  결정을 기다리는 대신 만들고, 결정 항목은 PENDING에 남겼다.
- **T5** CI 자동 로그인 — 사용자가 전용 테스트 계정 + 시크릿 2개를 만든 뒤.
- **T6** 44건 최초 실행 결과 기록 — T5 완료 후.

## 원래 Task (안) — 위 결과와 대조용으로 남긴다

### T1 — 이메일 로그인·가입 UI (사용자 요청 0번의 본체)

[`LoginForm.tsx`](../../apps/web/src/components/../app/login/LoginForm.tsx)에 이메일 폼을 더한다.

- **OAuth를 위에, 이메일을 아래에 둔다.** 기존 사용자는 전부 OAuth로 가입했으므로
  이메일 폼을 위에 놓으면 그 사람들이 "내 계정이 없어졌나" 하고 헷갈린다.
- 로그인·가입을 **한 폼에서 탭으로** 나눈다. 별 화면을 만들면 라우트가 늘고 CSP·정적 프리렌더
  함정을 다시 밟는다(이 저장소가 `/login`에서 **이미 두 번 밟은 부류** — L-CSP).
- **`/login`은 `force-dynamic`을 유지한다.** 정적으로 구워지면 nonce CSP가 스크립트를 전부
  막아 로그인이 완전 불능이 된다 — 이 저장소가 **실사용자에게 발견당한** 그 사고다.
  [Phase 38의 `buildStaticGuard`](../Status.md)가 이미 검사하므로 새로 만들지 않는다.
- 접근성: `<form>` + `type="email"` + `type="password"` + `autoComplete`(`email` / `current-password`
  / `new-password`). 오류는 `role="alert"`로 알린다.

**수용 기준**: 기존 Google·GitHub 버튼의 동작이 **한 글자도 바뀌지 않는다**(회귀 테스트로 잠금).

### T2 — 비밀번호 정책·상한 (공용 구현을 쓴다) — **완료 (2026-07-27)**

> 로그인 시도 상한을 공용 `allowRequest`로 붙였다(5분에 5번).
> **정직하게: 이건 보안 통제가 아니다.** 상한이 **그 탭의 메모리**에 살아서 새로고침하면
> 초기화된다 — 작정한 공격자는 그냥 지나간다. **실제 방어선은 Supabase Auth 자체의 상한**이고
> 이건 그 앞의 완충이다. 오타로 연달아 실패하는 사람에게 "잠시 뒤에"라고 알려 주는 쪽이 목적이다.
> 계획이 요구한 대로 **그 한계를 코드 주석에 적었고**, 적혀 있는지까지 테스트가 검사한다.
> 재구현 금지도 정적 검사로 잠갔다(자체 Map을 만들면 실패한다 — Phase 36 L-16 재발 방지).


- 비밀번호 최소 길이는 **Supabase 설정에서 정한다**(코드로 다시 검사하지 않는다 —
  두 벌이면 한쪽만 고쳐진다). 화면에는 그 기준을 **문구로만** 알린다.
- **로그인 시도 상한은 `@ldd/api`의 공용 `allowRequest`를 쓴다.**
  자체 `Map`으로 재구현하면 **CLAUDE.md 3-5절 최고 심각도 인벤토리 위반**이다 —
  이 저장소가 [Phase 36](phase_36.md)에서 정확히 그걸로 데였다(교훈 L-16).
- **[확인 필요] 그 상한은 서버 인스턴스 메모리에 산다** — Vercel이 인스턴스를 여럿 띄우면
  그만큼 느슨하다(`PENDING.md` 6번과 같은 한계). Supabase Auth 자체의 상한이 실제 방어선이고,
  우리 상한은 그 앞의 완충이다. **이 한계를 코드 주석에 적는다**(적지 않으면 다음 사람이 믿는다).
- 오류 문구는 **"이메일 또는 비밀번호가 올바르지 않습니다"** 하나로 통일한다.
  "없는 계정"과 "틀린 비밀번호"를 구분하면 **어느 이메일이 가입돼 있는지 알려주는 통로**가 된다.

### T3 — 비밀번호 재설정 (없으면 잠긴다)

이메일 로그인을 만들고 재설정을 안 만들면 **비밀번호를 잊은 사용자는 영구 잠긴다.**
OAuth 사용자는 이 문제가 없어서 지금까지 필요가 없었다.

- `resetPasswordForEmail` → 메일 링크 → `/auth/reset` 에서 새 비밀번호 설정.
- **[확인 필요 — 무료 티어 제약]** Supabase 내장 SMTP는 발송량 상한이 있다.
  `CONSTRAINTS_FREE_TIER.md`에 **이 항목이 없다**(실측: SMTP·메일 언급 0건).
  착수 시 실제 상한을 확인해 그 문서에 추가한다. 상한이 너무 낮으면
  **가입 시 메일 확인을 끄는 것**(Supabase 설정)이 대안이고, 그건 사용자 결정 사항이다.
- **하지 않을 것**: 자체 메일 발송 인프라. 무료 원칙과 충돌하고 Supabase가 이미 한다.

### T4 — 스킵 계약을 한 벌로 (Phase 40 T2 흡수)

같은 3줄(`AUTH_STATE_PATH` 계산 · `existsSync` · `test.skip`+`storageState`)이
**10개 스펙에 복사돼 있다**(실측). 이 Phase가 그 경로 규칙을 건드리므로 먼저 모은다.

- `e2e/authState.ts`에 한 벌로 모으고 10개 스펙이 그걸 쓴다.
- **동작은 한 글자도 바꾸지 않는다.** 착수 전/후로 `playwright test --list`의 개수가
  **62/44로 같아야 한다** — 그게 회귀 판정 기준이다.
- 왜 먼저인가: 지금 상태로 경로 규칙을 바꾸면 10곳을 고쳐야 하고, **한 곳을 빠뜨리면
  그 스펙만 조용히 스킵된 채 남는다.** Phase 32가 정리한 부류다.

### T5 — CI가 스스로 로그인한다 (44건 해금)

- `globalSetup`이 `E2E_EMAIL`·`E2E_PASSWORD`가 **있으면** `signInWithPassword`로 로그인해
  `e2e/.auth/user.json`을 만들고, **없으면 아무것도 하지 않는다**(지금과 동일하게 스킵).
- **미설정이 안전한 기본값**이어야 한다 — 시크릿이 없을 때 CI가 실패하면 안 된다
  (Phase 35 계정 삭제·Phase 40 T1이 세운 방식과 같다).
- **비밀번호가 로그에 남지 않게 한다.** 경로만 알리고 내용·자격증명은 찍지 않는다.
- 수용 기준:
  - 시크릿 없이 CI가 **지금과 똑같이** 통과한다 (44건 스킵 유지)
  - 시크릿을 넣으면 세션 파일이 생기고 **스킵이 멈춘다**
  - 틀린 비밀번호를 넣으면 **명확한 사유로 스킵**한다(실패가 아니라 "자격증명을 확인하라") —
    그러지 않으면 "자격증명 오류"와 "진짜 회귀"가 CI에서 같은 실패로 보인다

### T6 — 44건이 처음 도는 것을 회귀와 구분한다

세션이 생기면 44건이 자동으로 돈다. **여러 건 실패할 수 있다.**

- 그건 회귀가 아니라 **한 번도 실행된 적 없는 테스트가 처음 도는 것**이다.
  Phase 34(발표 모드)·Phase 38(404 CSP)의 스펙은 **작성만 되고 한 번도 실행되지 않았다** —
  실행되지 않은 테스트는 테스트가 아니라 문서다.
- 최초 실행 결과를 **파일로 먼저 저장한 뒤**(HD-010) 하나씩 처리한다.
  실패를 한꺼번에 고치려 하지 않는다.
- **44건을 통과시키는 것은 이 Phase의 수용 기준이 아니다.** 이 Phase의 기준은
  **"켜졌다"**다 — 몇 건이 실패하는지 숫자로 아는 것까지.

## 의존 계약

- **신규 의존성 0개.** `@supabase/supabase-js`가 이미 `signInWithPassword`·`signUp`·
  `resetPasswordForEmail`을 전부 갖고 있다. 폼은 우리 `Button`·`Input`을 쓴다.
- **마이그레이션 0개.** `profiles` 트리거가 `auth.users` 삽입에 붙어 있어
  **가입 경로와 무관하게 프로필이 만들어진다**([확인 필요] — `20260720100400_profiles_trigger.sql`이
  provider를 가리지 않는지 착수 시 확인. 가린다면 그때 마이그레이션이 필요하다).
- **시크릿을 저장소에 커밋하지 않는다.** `e2e/.auth/`는 `apps/web/.gitignore:47`에 있다(실측 확인).

## 사용자가 해야 하는 것 (코드로 못 하는 것)

1. **Supabase 대시보드 → Authentication → Providers → Email 켜기.**
   이걸 켜기 전까지 T1의 폼은 오류만 낸다.
2. **가입 시 메일 확인(Confirm email)을 켤지 결정.** 켜면 안전하지만 무료 티어 발송 상한에
   걸릴 수 있고, 끄면 아무 이메일로나 가입된다. **되돌리기 쉬운 설정이라 사용자 판단으로 남긴다.**
3. **e2e용 전용 테스트 계정을 하나 만들고** `E2E_EMAIL`·`E2E_PASSWORD`를 GitHub 시크릿에 등록.
   **본인 계정을 쓰지 말 것** — 인증 e2e는 매 푸시마다 실제 프로젝트에 투두·메모를 만들고 지우며,
   **실패하면 잔여 데이터가 남는다**(Phase 40이 이미 경고한 항목).
   → 완료 시 `PENDING.md` 2번을 "명령 한 번" 대신 "시크릿 2개 등록"으로 갱신한다.

## 하지 않을 것

- **`service_role`로 세션을 합성하는 것.** 그 키는 RLS를 통째로 우회하고, 테스트를 위해
  프로덕션 키를 CI에 넣는 것은 얻는 것보다 잃는 게 크다([Phase 35](phase_35.md)와 같은 판단).
  이제 이메일 로그인이 있으므로 **고려할 이유조차 없다.**
- **계정 연결(같은 이메일의 OAuth 계정과 이메일 계정 병합).** 요청 6-3이 정확히 그 일이고
  [Phase 49](phase_49.md)에 있다. 여기서 반쪽으로 만들면 **거기서 다시 짜게 된다.**
  이 Phase에서는 **Supabase 기본 동작을 그대로 두고, 그 동작이 무엇인지 실측해 기록한다**
  (같은 이메일로 가입 시도 시 무슨 일이 일어나는지 — 병합·거부·별 계정 중 무엇인가).
- **소셜 provider 추가**(카카오·네이버 등). 요청에 없다.
- **44건 전부 통과시키기.** T6 참조.
