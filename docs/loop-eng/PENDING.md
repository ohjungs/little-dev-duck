# 지금 사용자가 할 일 (2026-07-26 `/loop-eng` 세션 종합)

이 세션에서 코드로 끝낼 수 있는 건 다 했고, **남은 건 제가 못 하는 것들**입니다.
`Status.md`(1300줄 넘음)와 `manual-verification.md`(20항목)에 흩어져 있어 여기 모았습니다.
상세는 각 항목의 링크를 따라가면 됩니다.

---

## 1. DB 변경 3건 적용 — 명령 한 번

```
supabase db push
```

되돌리기 어려운 작업이라 **자율로 적용하지 않았습니다**(CLAUDE.md 5절). 롤백 스크립트는
`supabase/rollback/`에 같은 이름 `_down.sql`로 다 있습니다.

| 순위 | 마이그레이션 | 내용 | 지금 상태의 문제 |
|---|---|---|---|
| **높음** | `20260726110000_harden_security_definer` | `award_xp` 권한 결함 수정 | **로그인 없이도 남의 오리 XP·레벨·먹이를 바꿀 수 있음** |
| 중간 | `20260726100000_todo_recurrence` | 할 일 반복 컬럼 | 반복 기능이 동작하지 않음(나머지는 정상) |
| 낮음 | `20260726120000_rls_initplan_rest` | RLS 성능 + 외래키 인덱스 | 성능만, 동작엔 영향 없음 |

적용 뒤에 Supabase 어드바이저를 다시 돌리면 지적이 사라졌는지 확인됩니다.
`get_public_page`가 anon에 열린 것은 **의도된 설계**라 남는 게 정상입니다.
→ 상세: [manual-verification 17번](manual-verification.md), [14번](manual-verification.md)

---

## 2. e2e 로그인 세션 파일 한 번 만들기 — 39건이 대기 중

```
cd apps/web
pnpm exec playwright open http://localhost:3100/login --save-storage=e2e/.auth/user.json
```

브라우저가 뜨면 실제로 로그인하고 창을 닫으면 됩니다(포트는 **반드시 3100**).
→ 절차 원문: `apps/web/e2e/README.md`

지금은 로그인 뒤 화면 테스트 **39건이 전부 건너뛰어집니다.** 세션 파일이 생기면 그때부터
자동으로 돌아갑니다 — 이 세션에서 만든 것 중 아래가 그 대상입니다.

- 반복 할 일(주기 표시·선택 폭·완료 시 이동) 6건
- 마감일(표시·입력 폭·하루 밀림 금지) 4건
- 삭제 되돌리기(같은 항목 복귀·스크린리더·연속 삭제) 4건
- 기존 위젯·반응형 테스트 25건

---

## 3. 눈으로 봐주셨으면 하는 화면

제가 로그인할 수 없어 **코드·테스트는 통과했지만 실제 렌더는 못 봤습니다.**
날짜 관련 수정이 여러 번 있었으니 특히 아래를 봐주시면 좋겠습니다.

| 화면 | 확인할 것 | 왜 |
|---|---|---|
| 캘린더 위젯 | 일정 옆 날짜·D-day가 **맞는 날짜**인지 | 저장 방식을 바꾸면서 하루 밀리는 회귀를 냈다 고쳤음 |
| 통계 화면 | "이번 주 vs 지난 주", 연속 활동 일수 | 주간 경계가 하루 밀려 집계되던 것 수정 |
| 할 일 위젯 | 마감일 지정 → "오늘 마감" 필터에 걸리는지 | 필터는 있는데 입력 수단이 없던 것을 이번에 추가 |
| 뉴스 | GeekNews 기사에 **요약이 보이는지** | 요약이 항상 비어 있던 것 수정(실측 확인은 함) |

→ 상세 재현 절차: [manual-verification 15·18·19번](manual-verification.md)

---

## 4. 결정이 필요한 것 — 오리 코스튬

`duck_state.costume` 컬럼이 있고, "먹이"는 코드 주석상 **코스튬 구매 재화**이며,
`DECISIONS.md`도 "코스튬은 레벨 해금 + 먹이 구매"로 확정해 뒀습니다.
그런데 **저장만 되고 아무 데서도 읽히지 않습니다** — 선택 UI도 상점도 없습니다.
즉 **XP를 쌓아 먹이를 모아도 쓸 곳이 없습니다**(먹이 수치는 화면에 보입니다).

오리는 스프라이트 한 장으로 그려지므로 코스튬에는 **새 그림이 필요**하고, 이 저장소의 오리
외형은 사용자가 직접 만들어 넣어 온 영역이라 **제가 임의로 만들지 않았습니다.**

셋 중 하나를 정해 주시면 됩니다:
1. 코스튬 그림을 제공 → 해금·선택 UI를 만듦
2. 코스튬 대신 다른 보상으로 대체
3. 먹이 개념을 접음 (`ARCHITECTURE_DIAGRAMS.md`에도 "먹이 폐기 추정" 메모가 있음)

→ 상세: [manual-verification 20번](manual-verification.md)

---

## 5. 방문자 통계가 한 건도 안 쌓이고 있습니다 — 토글 하나

Vercel 대시보드 → 프로젝트 `web` → **Analytics 탭 → Enable**.

`<Analytics />`는 이미 모든 페이지에 붙어 있는데(`apps/web/src/app/layout.tsx:69`),
프로젝트에서 Web Analytics가 **꺼져 있어** 스크립트가 404입니다. Vercel API가 그대로
답했습니다 — `web_analytics_not_enabled`. 같은 프로젝트의 Speed Insights 스크립트는 200이라
인프라 문제가 아니라 이 기능만 꺼진 상태입니다.

지금까지 **페이지뷰·유입이 하나도 기록되지 않았습니다.** 덤으로 방문자마다 브라우저 콘솔에
404가 하나씩 남습니다. 켜면 그 시점부터 쌓이기 시작합니다(과거분은 복구되지 않습니다).

코드 쪽은 손댈 게 없어 그대로 뒀습니다 — `<Analytics />`를 떼면 나중에 켜도 안 걷힙니다.
→ 근거·재현: [screenshots/2026-07-26/public-surfaces/index.md](screenshots/2026-07-26/public-surfaces/index.md)

참고로 Speed Insights는 서버가 스크립트를 서빙하는데 앱이 안 쓰고 있습니다(`@vercel/speed-insights`
미설치). 쓰려면 의존성 추가가 필요해 **임의로 넣지 않았습니다** — 원하시면 말씀해 주세요.

---

## 6. 선택 사항 — 문서 정리

`docs/Status.md`가 **1348줄**입니다. 완료된 Phase 서술을 `History.md`로 옮기면 짧아지는데,
**문서 삭제는 임의로 하지 않았습니다.** 정리를 원하시면 말씀해 주세요.
