# /review 스냅샷 — 2026-07-21 (Phase 5 종료 시점)

대상: Phase 5 산출물 전체(커밋 `382dc57..HEAD`) — `apps/desktop/*`(Tauri 2 셸 + Rust `collector`),
`packages/core/src/domain/activity-daily.*`, `packages/api/src/activity.*`,
`apps/web/src/components/DesktopCollectorSync.tsx`, `apps/web/src/proxy.ts`(CSP),
`apps/web/src/app/login/*`(force-dynamic 분리), `apps/web/src/components/GithubContributionWidget.tsx`,
`supabase/migrations/20260721000000_activity_daily.sql`.

방식: 6개 차원(보안/Rust/TS·React/DB/테스트/규칙정합) 병렬 리뷰 → 각 발견을 독립 에이전트가
적대적으로 검증(반증 우선). 39개 서브에이전트, 원 발견 33건 → 확정/불확실 30건, 반증 3건.

DELTA: 직전 스냅샷(Phase 3, 2026-07-20-phase3.md)의 SEC-04(open redirect 방어적 명확성, DEFER)는
이번 Phase에서도 신규 리다이렉트 라우트를 추가하지 않아 재검토 대상 아님. `auth/callback`/`auth/logout`
불변.

## SEC- (배포 차단 판정 대상)

| ID | 등급 | 내용 | 판정 |
|---|---|---|---|
| SEC-09 | - | 로그 수집기 프라이버시: `collector/mod.rs`는 파일 내용을 전혀 안 읽고 mtime+확장자만 사용, 반환값은 `DailyCount{date,count}`뿐 — 경로/파일명/내용이 Rust 경계를 넘지 않음(DECISIONS.md 프라이버시 원칙 준수, 다중 에이전트 실측 확인) | 통과 |
| SEC-10 | - | RLS 격리: `activity_daily`에 select/insert/update/delete 4개 정책이 전부 `user_id=auth.uid()`로 스코프됨(누락 없음), insert/update는 `with check`까지. `upsertActivityDaily`는 `auth.getUser()`(JWT 서버 검증)로 user_id를 고정해 교차 테넌트 위조 쓰기 불가 | 통과 |
| SEC-11 | - | 시크릿: Rust 바이너리에 Supabase 자격 증명 없음(업로드는 로그인된 WebView JS가 수행). 세션/토큰이 Rust로 넘어가지 않음(invoke는 인자 없음) 실측 확인 | 통과 |
| SEC-12 | - | CSP/미들웨어 생존: "proxy.ts가 middleware.ts 부재로 죽은 코드"라는 SEC-HIGH 주장은 **반증됨** — Next.js 16은 `proxy.ts`+export `proxy`를 native 미들웨어 규약으로 인식(Next 15 이하 기준의 오판). 프로덕션 응답에 CSP+6개 헤더 실림을 curl로 이미 실측 | 반증(통과) |
| SEC-13 | - | 롤백 가능성: `activity_daily` 마이그레이션에 down 스크립트 동반(`rollback/20260721000000_activity_daily_down.sql`), 프로덕션 적용 완료 | 통과 |

**배포 차단 여부: 없음.** 확정 30건 전부 REF-MEDIUM/REF-LOW.

## REF-MEDIUM (이번 세션에서 수정)

| ID | 파일 | 내용 | 조치 |
|---|---|---|---|
| REF-M1 | `collector/mod.rs:64` | 동기 Tauri 커맨드가 UI 스레드에서 인라인 실행 → 세션 파일 누적 시 위젯 프리즈(설치된 tauri 매크로 소스로 확증) | **수정** — `#[tauri::command(async)]`로 워커 스레드 실행, 파일당 progress emit도 완료 1회로 축소 |
| REF-M2 | `activity.ts:31` | upsert의 UPDATE 경로에서 `updated_at`이 최초 INSERT 시각에 고정(DB 트리거 부재, todos/memos는 클라이언트가 직접 세팅하는데 activity만 누락) | **수정** — rows에 `updated_at: now` 추가(기존 패턴과 일치) |
| REF-M3 | `collector/mod.rs:59` | 자정/UTC-오프셋 회귀 버그를 잡는 테스트 없음 — session_date를 UTC로 되돌려도 5개 전부 통과 | **수정** — 순수 함수 `format_local_date` 추출 + 두 오프셋 자정 경계 테스트로 회귀 고정 |
| REF-M4 | `activity.test.ts:14` | mock이 upsert 인자를 통째로 무시 — user_id 스탬핑/onConflict/테이블명 무검증(동어반복 happy-path) | **수정** — `vi.fn` spy로 인자 캡처, user_id·onConflict·테이블명·updated_at 단언 추가 |
| REF-M5 | `collector/mod.rs:146` | 집계 테스트가 프로덕션 코드 사본을 검증, `aggregate`/`DailyCount` 변환이 한 번도 실행 안 됨 + 복수 날짜 버킷 미검증 | **수정** — 순수 `aggregate(&[PathBuf])` 추출(프로덕션·테스트 공용), `set_modified`로 3일 차 mtime 심어 날짜별 분리 집계 검증 |
| REF-M6 | `phase_05.md:159` | 문서가 CSP를 `script-src 'self'`(nonce 불필요)로 과소 기술 — 실제는 nonce 기반(strict-dynamic), 같은 문서 T4가 스스로 반증 | **수정** — 스테일 문구 제거, nonce 전환은 T4 절이 정확히 기술 |

수정 후 검증: `cargo test` 6/6(회귀 고정 2건 신규), `pnpm --filter @ldd/api test` 28/28, 전체 lint/build.

## REF-LOW (후속 과제로 유지)

24건 중 다수가 이미 `phase_05.md` MEDIUM/LOW 후속 목록에 등재된 항목의 재확인(중복):
symlink 미검증(`find_session_files`), 동기화 실패 무음 처리(사용자 에러 상태 부재),
`upsertActivityDaily` 입력단 zod 미검증, count 상한 부재, `capabilities/default.json` 실효성 재검토.

신규 REF-LOW(미등재, 다음 하드닝 라운드 후보):
- `collector/mod.rs:60` 과거 mtime에 현재 오프셋 적용 → DST 경계 하루 오차(KST는 DST 없어 무영향, 코드 주석으로 한계 명시함)
- `collector/mod.rs:38` 권한 거부/stat 실패 세션이 로그 없이 누락(언더카운트, panic은 없음)
- `Cargo.toml` `serde_json` 미사용 직접 의존성(Tauri가 transitive로 끌어옴, 제거 이득 작음)
- `DesktopCollectorSync.tsx:39` 언마운트 레이스로 이벤트 리스너 누수 가능, 첫 await가 try 밖(미처리 rejection)
- `proxy.ts:90` 인증 allowlist를 `startsWith`로 판정 → 경로 프리픽스 오매칭 여지
- `LoginForm.tsx:12` `signInWithOAuth` 오류/로딩 미처리로 클릭 무반응 가능
- `GithubContributionWidget.tsx:104` 색상 단일 채널 인코딩(접근성 — 색맹 대비 부족)
- `Status.md`/`phase_05.md` 간 T4 상태 순간 미동기화(이 커밋으로 해소)
- `activity-daily.ts` core가 zod를 import — "core 의존성 0"이라는 문구와의 해석 차이(zod는 유일 허용 예외로 이미 전 Phase에서 채택, 문구 정합만 필요)

## 반증(REJECTED) 3건

| 주장 등급 | 내용 | 반증 근거 |
|---|---|---|
| SEC-HIGH | proxy.ts CSP/인증이 프로덕션에서 안 도는 죽은 코드(middleware.ts 부재) | Next 16이 `proxy.ts`를 native 인식. 프로덕션 curl로 CSP 6헤더 실측됨 |
| REF-HIGH | `current_local_offset()` 실패 시 UTC 대체로 자정 수정이 멀티스레드 Unix에서 무효화 | 고정된 크레이트 버전에서 전제가 거짓(local-offset 피처 활성) |
| REF-LOW | API 응답 zod 미적용 `as` 캐스팅으로 셀 렌더 크래시 | 핵심 재현 시나리오 성립 안 함 |

## 종합 판정

SEC- 배포 차단 없음. Phase 5 종료 가능. 실사용 검증(위젯 로그인 → `activity_daily` 반영)은
사용자 확인 완료. REF-MEDIUM 6건은 본 세션에서 수정(= notion-gap-analysis 7절 "Phase 6 전" P1
하드닝의 일부), 잔여 REF-LOW는 phase_06.md 착수 조건 및 후속 하드닝 라운드로 이월.
