# 리뷰 스냅샷 — Phase 9 워크스페이스 코어 (2026-07-22)

immutable. `/loop /next-step`(ultracode) 자율 진행 중, Phase 9 전체 코드(9 슬라이스, 20파일)를 배포 후
워크플로 하네스로 리뷰. 5개 렌즈 병렬(React/보안/마이그레이션/통합/엣지) × 파일 리뷰 → 발견별 적대적 검증
(총 36 서브에이전트). 확정 14건(HIGH 5·MEDIUM 5·LOW 4) 전건 수정. 오탐 판정분은 미반영.

배포차단(SEC- 등급)은 0건. 확정 결함은 데이터 정합성/UX/성능 계열이며 모두 같은 세션에서 수정(커밋 43c7330).

## HIGH (5) — 전건 수정

| # | 파일 | 결함 | 수정 |
|---|------|------|------|
| H1 | VersionHistory.tsx | 버전 복원이 대기 중 디바운스 자동저장을 취소하지 않아, confirm 블로킹 중 만료된 저장 콜백이 복원과 경쟁 → 복원이 조용히 무효화 | 복원 확인창 '전에' 상위 타이머 취소(onBeforeRestore 콜백을 PageEditor에서 주입) |
| H2 | 20260722040000 / pages.ts | 페이지 cascade 삭제 시 자식 임베딩이 RAG에 유령 잔존(embeddings.source_id는 pages FK 없음) → 삭제된 본문이 답변에 노출 | pages BEFORE DELETE 행 트리거 신설(20260722070000) — cascade 자식 행까지 발화해 embeddings를 같은 트랜잭션에서 정리 |
| H3 | PageWorkspace.tsx | handleSaved가 title만 갱신, content 스냅샷은 초기 로드값에 고정 → 페이지를 오갔다 돌아오면 stale content로 리마운트 후 최신 저장분 덮어쓰기(무음 데이터 손실) | onSaved가 서버 canonical content까지 전달, handleSaved가 content도 패치 |
| H4 | core/page.ts | extractPlainText가 테이블 셀(tableContent 객체)·미디어 캡션(props.caption) 미순회 → 테이블 위주 페이지 plain_text 비어 검색·RAG에서 사라짐 | tableText 헬퍼로 rows→cells 순회 + walkBlocks에서 props.caption 추출 + 회귀 테스트 |
| H5 | PageEditor.tsx | 언마운트(페이지 전환) cleanup이 대기 중 저장을 flush 없이 clearTimeout만 → 디바운스 창(800ms) 안에 전환하면 마지막 편집 유실 | cleanup에서 flushPendingSave로 즉시 발화(발화 시점 timer=null로 중복 방지) |

## MEDIUM (5) — 전건 수정

- M6 CommandPalette: 디바운스 검색이 out-of-order 응답에 무방비(느린 옛 요청이 최신 결과 덮어씀) → latestQuery ref로 stale 응답 폐기.
- M7 PageWorkspace/TrashView: 낙관적 삭제 롤백이 통짜 스냅샷 복원 → 동시 삭제 시 이미 처리된 항목 부활. 함수형 단일 항목 재삽입으로 교체.
- M8 TrashView purge: 하위 페이지 임베딩 미정리(= H2와 동일 근본원인). 위 DB 트리거로 해소.
- M9 VersionHistory: 복원이 reindex를 안 해 인덱스가 복원 이전으로 stale. updatePage 후 서버 plainText로 reindex(reload 전 await).
- M10 reindex-all: memo→…→page 순 concat 후 slice(200) → 다른 소스 200+ 시 page 통째 누락. 소스 라운드로빈 인터리브로 공평 분배.

## LOW (4) — 전건 수정

- L11 page-attachments 버킷: content-type 미검증(SVG/HTML 인라인 가능) → allowed_mime_types(이미지 4종)+file_size_limit(10MB) + 클라 가드. (리뷰의 저장형 XSS 프레이밍은 오탐 — self-XSS·공유 기능 부재로 판정, 하드닝만 수용.)
- L12 createPageVersion: 스냅샷 title/content를 클라 입력으로 받아 무결성/소유권 미강제 → 서버 페이지를 RLS SELECT로 읽어 스냅샷 파생(입력을 {pageId}로 축소).
- L13 pages RLS: bare auth.uid()(행별 재평가) → (select auth.uid())로 initplan 캐싱(auth_rls_initplan advisor).
- L14 safeFileName: 공백만 제목이 'page' 폴백을 못 타 '   .md' → trim-first 폴백.

## 오탐(미반영) 요지

- page-attachments public 버킷 "전세계 읽기" = 개인 워크스페이스에서 추측 불가 UUID 경로 + RLS 본인 폴더 쓰기로 허용 범위. 저장형 XSS는 공유 기능 부재·img 렌더·크로스오리진 세션 격리로 성립 안 함(하드닝만 수용).
- createPageVersion 교차 사용자 오염 = listPageVersions가 RLS로 본인 행만 반환해 기밀성 침해 없음(정합성 결함으로만 LOW 인정).

## 검증

수정 후 core 98 / api 95 / ai 6 tests + web build GREEN + 로컬 full eslint(apps/web) exit 0. CI(lint+e2e) green(43c7330). 신규 마이그레이션 1건(트리거)으로 db push 대기 5건.
