# /review 스냅샷 — 2026-07-20 (Phase 3 종료 시점)

대상: `packages/mascot/*`, `apps/web/src/components/DuckWidget.tsx` (커밋 3b34286).
같은 날짜에 Phase 1 스냅샷(2026-07-20.md)이 이미 있어 파일명을 phase3로 구분(immutable 원칙상
기존 파일 수정 대신 신규 생성).

DELTA: 직전 스냅샷(Phase 1, 2026-07-20.md)의 SEC-04(open redirect 방어적 명확성, DEFER)는 이번
Phase 범위 밖(새 리다이렉트 라우트 없음) — 재검토 대상 아님.

## SEC- (배포 차단 판정 대상)

| ID | 등급 | 내용 | 판정 |
|---|---|---|---|
| SEC-05 | - | 시크릿 노출: mascot 패키지는 서버 키/외부 API 호출 없음(순수 클라이언트 렌더링) | 통과(해당 없음) |
| SEC-06 | - | RLS: Phase 3에서 신규 테이블 없음(duck_state는 Phase 3에서 사용 안 함, YAGNI로 보류) | 해당 없음 |
| SEC-07 | - | 롤백 가능성: 신규 마이그레이션 없음 | 해당 없음 |
| SEC-08 | - | `post-redirect-get` 재발 검사: `NextResponse.redirect` 호출 4곳 중 상태코드 없는 2곳은
  `auth/callback/route.ts`의 `GET` 핸들러 — anti-pattern 문서에 명시된 예외(GET은 원래도 GET
  유지)와 일치, 재발 아님 | 통과 |

**배포 차단 여부: 없음.**

## REF- (과잉구현/인벤토리)

| ID | 등급 | 내용 | 판정 |
|---|---|---|---|
| REF-04 | - | 인벤토리 위반 없음 — `Duck.tsx`가 `packages/ui`의 Button/Card 등을 재구현하지 않음.
  캐릭터 고유 색상(`COLOR_BODY` 등)은 `packages/ui` 토큰의 **원천**이지 재구현이 아님(설계상 방향) | 통과 |
| REF-05 | - | 과잉구현(ponytail 관점): 플레이스홀더 도형은 필요한 만큼만(sphere/cone/torus 8개 메시), 애니메이션
  라이브러리 추가 없이 `useFrame` 감쇠로 자체 구현 — 최소 구현 원칙 부합 | 통과 |

## DX/성능

| ID | 내용 | 판정 |
|---|---|---|
| DX-04 | `any`/`console.log`/`TODO`/`FIXME`/빈 catch — mascot+DuckWidget grep 결과 0건 | 통과 |
| DX-05 | 파일 크기: 최대 147줄(Duck.tsx) — 기준(800줄) 대비 여유 큼 | 통과 |
| DX-06 | (신규 발견, LOW) `Duck.tsx`의 말풍선 인라인 스타일이 `var(--ldd-color-accent, #A99C65)`처럼
  CSS 변수 폴백값을 하드코딩 — 오늘 테마 accent를 `#D97757`로 변경(별도 커밋)하면서 이 폴백값만
  구식으로 남음. 실제 렌더링에는 영향 없음(CSS 변수가 항상 정의돼 있어 폴백 경로 자체가 안 탐) | SKIP — 방어적 폴백값 불일치일 뿐 실사용 경로 아님. mascot 관련 다음 수정 때 같이 정리 |

## 재발/이력 확인

- 구현 세션 자체 코드 리뷰에서 발견된 HIGH 2건(클릭 핸들러 `stopPropagation` 누락,
  `clickCount` post-increment로 인한 문구 off-by-one)은 커밋 3b34286에 포함된 수정으로 이미 해소
  — 이번 DETECT에서 재발 없음 확인(코드 재확인 완료).

## 종합 판정

SEC- 배포 차단 없음. Phase 3 종료 가능. 사용자 클릭 검증은 별도 세션에서 완료된 것으로 간주.
